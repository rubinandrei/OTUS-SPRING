spring:
  datasource:
    initialization-mode: always
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    data: test-data.sql
logging:
  level:
    root: info
app:
  port_server: 7072
  port_client: 8082

#jdbc:h2:tcp://localhost:7072/mem:book-library
#http://localhost:8082
